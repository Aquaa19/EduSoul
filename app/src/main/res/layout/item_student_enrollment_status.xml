<?xml version="1.0" encoding="utf-8"?>
<!--
    Item layout for displaying a student in the enrollment management screen.
    Shows student name and an action button (Enroll/Unenroll).
    Tint for ImageView changed to ?attr/colorPrimary for better theme compatibility.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingEnd="8dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:gravity="center_vertical"
    android:background="?android:attr/selectableItemBackground">

    <ImageView
        android:id="@+id/imageViewStudentEnrollmentIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_student"
        app:tint="?attr/colorPrimary"
    android:layout_marginEnd="12dp"
    android:contentDescription="Student Icon"/>

    <TextView
        android:id="@+id/textViewStudentNameEnrollment"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1"
        tools:text="Student Full Name"/>

    <Button
        android:id="@+id/buttonEnrollmentAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        tools:text="Enroll"
        android:layout_marginStart="8dp"/>

</LinearLayout>
