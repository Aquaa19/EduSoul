<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/ai_assistant_dark_purple">

    <ScrollView
        android:id="@+id/chatScrollView"
        android:layout_width="match_parent"
        android:layout_height="200dp"  android:layout_weight="1"
        android:fadeScrollbars="false"
        android:scrollbarStyle="outsideOverlay"
        android:fillViewport="true">

        <TextView
            android:id="@+id/chatHistoryTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:text="AI: Hello! How can I help you today?"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body1"
            android:textColor="@color/ai_assistant_yellow"
            android:overScrollMode="always" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">

        <EditText
            android:id="@+id/commandEditText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Type your command..."
            android:inputType="textCapSentences"
            android:maxLines="3"
            android:minHeight="48dp"
            android:textColor="@color/ai_assistant_yellow"
            android:textColorHint="@color/ai_assistant_light_green" />
        <Button
            android:id="@+id/sendButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Send"
            app:backgroundTint="@color/ai_assistant_green"
            android:textColor="@color/white"
            android:layout_marginStart="8dp" />

        <ImageButton
            android:id="@+id/voiceInputButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Voice Input"
            app:tint="@color/ai_assistant_light_green"
            android:src="@drawable/ic_mic" />

    </LinearLayout>

</LinearLayout>