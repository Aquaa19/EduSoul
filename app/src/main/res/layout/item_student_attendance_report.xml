<?xml version="1.0" encoding="utf-8"?>
<!--
    Item layout for displaying a student's attendance status for a specific session or day.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground">

    <TextView
        android:id="@+id/textViewStudentNameReport"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1"
        tools:text="Student Name Here"/>

    <!-- This RecyclerView will list sessions and their attendance for this student on the selected day -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewStudentSessionAttendance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        tools:listitem="@layout/item_session_attendance_detail"
        tools:itemCount="2"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"
        android:layout_marginTop="8dp"/>

</LinearLayout>
