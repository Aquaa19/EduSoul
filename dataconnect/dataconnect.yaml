specVersion: "v1"
serviceId: "edusoul"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "edusoul-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
